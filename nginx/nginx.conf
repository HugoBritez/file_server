# Configuración simplificada de nginx para debug
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Logs
    access_log /var/log/nginx/access.log;

    # Upstream para el API Go
    upstream go_api {
        server api:3000;
    }

    # Servidor HTTP (redirección a HTTPS)
    server {
        listen 4040;
        server_name _;
        return 301 https://$server_name:4040$request_uri;
    }

    # Servidor HTTPS principal
    server {
        listen 4040 ssl;
        server_name _;

        # Configuración SSL - USANDO TUS CERTIFICADOS
        ssl_certificate /etc/nginx/ssl/server.cer;
        ssl_certificate_key /etc/nginx/ssl/server.key;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        # API Routes - DEBE IR PRIMERO
        location /api/ {
            proxy_pass http://go_api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Health check
        location /health {
            return 200 "nginx OK\n";
            add_header Content-Type text/plain;
        }

        # Archivos estáticos
        location /static/ {
            alias /var/www/static/;
            expires 1y;
            add_header Cache-Control "public, immutable";
            try_files $uri =404;
        }

        # Frontend
        location / {
            root /var/www/frontend;
            index index.html;
            try_files $uri $uri/ /index.html;
        }
    }
} 